import 'package:flutter/material.dart';

class Pneumonia1 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pneumonia'),
      ),
    );
  }
}
